## Sample .lo_tools_conf file
## This file should locate in your home directory
## Uncomment and define the variables you wish to set

## Define default bucket name
#fixed_bucket=1         ## 1: use bucket name from bucket_name variable, 0: assign bucket name automatically.
#bucket_name="name"     ## bucket name without "/" characters
##
## Modes
#mode="s3cmd"           ## options: s3cmd
##
## Verbosity
#silent=0               ## 0: normal output, 1: less output
##
## Directory for temporary files
#tmp_root=/directory
##
## Check if there is enough space for temporary files
#free_space_check=1     ## 0: no check, 1: do check
##
## Override existing objects
#override_mode=0        ## 0: halt if object exists, 1: override
##
## Store each file as a separate object
#asis_mode=0            ## 0: store directories as tar files, 1: store each file as an object
##
## Compression
#compression=1          ## 0: no compression, 1: compress with zstdmt
##
## Encryption
#encrypt=""             ## options: ""(no encryption), gpg, crypt4gh
#public_key=""          ## Location of crypt4gc encryption key
##
## Create metadata object (_ometa)
# include_ometa=1       # 1: create metadata object, 0: don't create metadataobject
