name: lumio
channels:
  - conda-forge
  - defaults
dependencies:
  - zstd
  - python
  - pip
  - s3cmd
  - rclone
  - restic
  - pip:
    - python-openstackclient
    - python-swiftclient
    - python-rclone
    - crypt4gh
